<div class="nojs">
  <p>This contact form requires JavaScript. Enable it to send me a message.</p>
</div>
<div id="contact-wrapper">
  <div id="contact-form-messages"></div>
  <form id="contact-form" method="post" role="form">
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <fieldset>
      <div class="field perspective">
        <label class="title" for="name">Name</label>
        <input id="name" name="name" type="text" placeholder="Your name" required="required">
      </div>
      <div class="field perspective">
        <label class="title" for="email">Email</label>
        <input id="email" name="email" type="text" placeholder="Your email" required="required">
      </div>
      <div class="field perspective">
        <label class="title" for="message">Message</label>
        <textarea id="message" name="message" placeholder="Enter your message here" rows="3"
          required="required"></textarea>
      </div>
      <div class="field perspective">
        <div class="title"><img id="captcha-img" src="{{captchaBase64}}" title="captcha">
          <a id="captcha-refresh-btn" title="Refresh Captcha">
          </a>
        </div>
        <input id="captcha" name="captcha" type="text" placeholder="Type what you see above" required="required">
      </div>
      <div class="field perspective">
        <button id="contact-form-submit-btn" name="submit" type="submit">
          Submit
        </button>
      </div>
    </fieldset>
  </form>
  <div>
    <p>Messages are encrypted using my <a href="/files/2B7340DB13C85766.asc" target="_blank">PGP
        public key</a>.</p>
    <p>You will receive a confirmation email if everything worked out!</p>
    <em>(Check your junk folder if you don't see it within a minute or so)</em>
  </div>
</div>